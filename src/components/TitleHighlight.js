import React from 'react'

export default function TitleHighlight({title}) {
    return (
        <div className="title-highlight">
             <h2 className="section-title">{title}</h2>
        </div>
    )
}
